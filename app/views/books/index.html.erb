<h1>Books</h1>
<table>
	<thead>
		<tr>
			<th>Title</th>
			<th>Boby</th>
			<th colspan="3"></th>
		</tr>
	</thead>
	<tbosy>
		<% @books.each do |book| %>
		<tr>
			<td><%=book.title %></td>
			<td><%=book.body %></td>
			<td>Show</td>
			<td>Edit</td>
			<td><%= link_to "Destroy", book_path(book.id), method: :delete, "date-confirm" => "本当に削除しますか？" %>
			</td>
		</tr>
		<% end %>
</table>


<h2>New Book</h2>
<%= form_for(@book) do |f| %>
   <h3>Title</h3>
   <%=f.text_field :title %>

   <h3>Boby</h3>
   <%=f.text_area :body %>

   <%= f.submit 'Create Book' %>
<%end%>